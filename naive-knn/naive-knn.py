import numpy as np
import pandas as pd

# using euclidean distance to find similarity between samples
# weights should be inversely proportional to the distances
# classification/regression must be informed as a parameter to the method

def predict(dataframe,k=3,d=0,split=0.8):
	return("foo")


d = np.array([1.0,2.0])